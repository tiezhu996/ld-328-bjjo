<!-- 改编自：农业与生活服务主题项目提示词/ld-328/ld-328.md；后端由 Django 4 + Django REST Framework 改造为 Go 1.22 + Gin + GORM -->
# ld-328 易腐食品保质期追踪

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**易腐食品保质期追踪（CyFreshFood）** 面向家庭和小型餐饮的食品保质期管理工具，帮助用户记录食品入库信息、自动计算保质期剩余天数、及时提醒临期食品，减少食物浪费。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户）**
  - 字段：id, phone, password_hash, name, avatar, role（Admin/Member）, created_at
  - 关键关系：用户可加入多个 FamilyGroup，创建多个 FoodItem 和 ConsumptionRecord
  - 业务动作：注册/登录、修改资料、上传头像
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/repository/user_repository.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/userStore.ts → frontend/src/pages/Profile.tsx**

- **FamilyGroup（家庭组）**
  - 字段：id, name, owner_id, created_at
  - 关键关系：1 个家庭组 → 多个 FamilyMember 和 FoodItem
  - 业务动作：创建家庭组、邀请成员、设置成员权限
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **FamilyMember（家庭成员）**
  - 字段：id, family_id, user_id, role（Admin/Member）, joined_at
  - 关键关系：关联 FamilyGroup 与 User
  - 业务动作：加入/退出家庭组、权限变更、操作人展示
  - **此实体必须贯穿全栈**

- **FoodItem（食品）**
  - 字段：id, family_id, name, category（生鲜/乳制品/熟食/烘焙/冷冻等）, production_date, shelf_life_days, quantity, unit, storage_location（冰箱/pantry/冷冻室等）, opened_at, expiry_date, status（Fresh/Expiring/Expired/Consumed）, image_url, creator_id
  - 关键关系：隶属于 FamilyGroup，拥有多条 ConsumptionRecord
  - 业务动作：录入食品（含 CSV 批量导入）、编辑、消耗、自动归档
  - **此实体必须贯穿全栈**

- **ConsumptionRecord（消耗记录）**
  - 字段：id, food_item_id, quantity, consumed_at, user_id
  - 关键关系：隶属于 FoodItem，记录操作人 User
  - 业务动作：记录消耗、更新库存余量、查看历史与频率分析
  - **此实体必须贯穿全栈**

- **Notification（提醒通知）**
  - 字段：id, family_id, food_item_id, type（Expiring/Expired）, title, content, is_read, send_at, read_at
  - 关键关系：隶属于 FoodItem 与 FamilyGroup
  - 业务动作：临期/过期扫描、站内消息、邮件模拟发送
  - **此实体必须贯穿全栈**

- **Recipe（简易食谱建议）**
  - 字段：id, name, ingredients, description, suitable_category
  - 关键关系：按即将过期食品类别关联
  - 业务动作：智能推荐优先食用食品与搭配建议
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/dashboard（保质期状态看板）**
  - 消费实体：FoodItem、Notification、ConsumptionRecord
  - 关键交互：卡片式展示全部食品（按剩余天数着色：绿色充裕 / 黄色 3 天内临期 / 红色已过期）、临期横幅提醒、按存放位置/类别/状态筛选
  - 共享组件：`<FreshnessBadge>`、`<RemainingDaysBar>`

- **/foods（食品管理）**
  - 消费实体：FoodItem、FamilyGroup
  - 关键交互：食品列表、新增食品表单、CSV 批量导入、食品详情（历史消耗记录）
  - 共享组件：`<FreshnessBadge>`（与 /dashboard 共用）、`<FoodCard>`

- **/consumptions（消耗记录）**
  - 消费实体：ConsumptionRecord、FoodItem
  - 关键交互：记录消耗、库存余量更新、历史消耗记录、消耗频率分析
  - 共享组件：`<FreshnessBadge>`、`<EmptyState>`

- **/statistics（分类统计与报表）**
  - 消费实体：FoodItem、ConsumptionRecord
  - 关键交互：各类食品消耗比例图、每月浪费金额估算、最常购买/最常浪费食品 Top 10、按月份筛选并导出 PDF
  - 共享组件：`<EmptyState>`（与 /consumptions 共用）、`<FoodCard>`

- **/family（家庭多成员共享）**
  - 消费实体：FamilyGroup、FamilyMember、User
  - 关键交互：创建/加入家庭组、成员管理、权限设置、操作人记录
  - 共享组件：`<MemberAvatar>`、`<EmptyState>`

- **/recommendations（智能推荐）**
  - 消费实体：FoodItem、Recipe
  - 关键交互：即将过期食品优先食用列表、简易食谱搭配建议
  - 共享组件：`<FoodCard>`（与 /statistics 共用）、`<FreshnessBadge>`

### 4. 横切关注点（≥ 2 个）

- **认证授权（JWT + RBAC）**
  - 触达文件层：
    - 数据库：`users` 表含 role 字段，`family_members` 表含家庭角色
    - 后端：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中家庭组权限设置等接口需 `AuthRequired()` 和 `RequireRole()`
    - 前端：`router/guards.tsx`、`components/common/RoleGuard.tsx`、`stores/authStore.ts`

- **定时任务（临期扫描 + 邮件模拟）**
  - 触达文件层：
    - 后端：`internal/service/reminder_service.go`、`internal/service/reminder_scheduler.go`、`internal/service/notification_sender.go`、`internal/util/scheduler.go`
    - 配置：`internal/config/config.go`（Redis 连接、扫描周期）
    - 前端：`stores/notificationStore.ts`、`pages/Dashboard.tsx` 横幅

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - 前端：`utils/request.ts` 拦截器、`components/common/ErrorBoundary.tsx`

### 5. 共享枚举/常量（≥ 2 组）

- **FoodCategory（食品类别）**
  - 值：FRESH = 'fresh', DAIRY = 'dairy', COOKED = 'cooked', BAKERY = 'bakery', FROZEN = 'frozen', OTHER = 'other'
  - 前端定义位置：`frontend/src/constants/food.ts`
  - 后端定义位置：`backend/internal/constants/food.go`
  - 出现位置：FoodItem 模型、FoodService、FoodCard、看板筛选器、统计分组、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **FreshnessStatus（新鲜度状态）**
  - 值：FRESH = 'fresh', EXPIRING = 'expiring', EXPIRED = 'expired', CONSUMED = 'consumed'
  - 前端定义位置：`frontend/src/constants/food.ts`
  - 后端定义位置：`backend/internal/constants/food.go`
  - 出现位置：FoodItem 模型、FoodService 状态机、FreshnessBadge、RemainingDaysBar、看板着色、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **UserRole（用户角色）**
  - 值：ADMIN = 'admin', MEMBER = 'member'
  - 前端定义位置：`frontend/src/constants/user.ts`
  - 后端定义位置：`backend/internal/constants/user.go`
  - 出现位置：User 模型、FamilyMember 模型、RBAC 中间件、路由权限、前端按钮显隐、路由守卫
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<FreshnessBadge>`：被 /dashboard、/foods、/consumptions、/recommendations 共用
- `<RemainingDaysBar>`：被 /dashboard 使用（剩余天数进度条）
- `<FoodCard>`：被 /foods、/statistics、/recommendations 共用
- `<MemberAvatar>`：被 /family 使用
- `<EmptyState>`：被 /consumptions、/statistics、/family 等多个列表页共用

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useFreshnessStats()`：新鲜度状态统计与看板着色
- `usePagination()`：通用分页
- `utils/calculateRemainingDays.ts`：根据到期日计算剩余天数
- `utils/dateFormat.ts`：日期时间格式化

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户角色校验家庭组权限
- `error_handler.go`：统一错误响应格式
- `rate_limiter.go`：接口限流
- `request_logger.go`：请求日志（method、path、status、latency）

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`cyfreshfood`
- **前端端口**：18628
- **后端端口**：19628
- **前端框架**：React 18 + TypeScript
- **UI 库**：Ant Design
- **图表库**：ECharts
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL
- **任务调度**：Go `time.Ticker`/`goroutine` + Redis（`github.com/redis/go-redis/v9`）
- **ORM**：GORM
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/postgres`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`、`github.com/redis/go-redis/v9`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # user.ts, family.ts, foodItem.ts, consumption.ts, notification.ts, recipe.ts
├── stores/           # authStore.ts, userStore.ts, familyStore.ts, foodStore.ts, notificationStore.ts
├── types/            # 共享类型定义
├── components/common/# FreshnessBadge, RemainingDaysBar, FoodCard, MemberAvatar, EmptyState, RoleGuard, ErrorBoundary
├── hooks/            # useFreshnessStats.ts, usePagination.ts
├── pages/            # Dashboard, FoodManage, ConsumptionManage, Statistics, FamilyManage, Recommendations, Profile
├── router/           # index.tsx + guards.tsx
├── utils/            # calculateRemainingDays.ts, dateFormat.ts, request.ts
└── constants/        # food.ts, user.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB/Redis、装配依赖、启动 Gin 与定时任务
└── internal/
    ├── config/                  # config.go（数据库连接、Redis、JWT、限流、扫描周期、端口等）
    ├── model/                   # 按实体分文件：user.go, family_group.go, family_member.go, food_item.go, consumption_record.go, notification.go, recipe.go
    ├── repository/              # 按实体分文件：user_repository.go, family_group_repository.go, family_member_repository.go, food_item_repository.go, consumption_record_repository.go, notification_repository.go, recipe_repository.go
    ├── service/                 # 按实体分文件：user_service.go, family_group_service.go, family_member_service.go, food_item_service.go, consumption_record_service.go, notification_service.go, recipe_service.go, reminder_service.go, reminder_scheduler.go, notification_sender.go, stats_service.go
    ├── handler/                 # 按实体分文件：user_handler.go, family_group_handler.go, family_member_handler.go, food_item_handler.go, consumption_record_handler.go, notification_handler.go, recipe_handler.go, stats_handler.go
    ├── router/                  # router.go + 按实体分文件：users.go, family_groups.go, family_members.go, food_items.go, consumption_records.go, notifications.go, recipes.go, stats.go
    ├── middleware/              # auth.go, rbac.go, error_handler.go, rate_limiter.go, request_logger.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # food.go, user.go, reminder.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, formatters.go, app_error.go, food_calculator.go, scheduler.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `FOOD_CONSUME_SUCCESS`、`FOOD_EXPIRY_SCAN_FAILED`
   - 任何业务字段变更（如 FoodItem 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"FoodItem[id=123] consume failed: quantity exceeds stock"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、剩余天数文本、新鲜度状态文本、类别文本、存放位置文本、角色文本
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **新鲜度状态机必须跨多处定义**
   - FreshnessStatus 的流转规则在 service 状态机、前端看板着色、日志模板、错误码、formatters 中同时存在
   - 新增一个状态值需要修改至少 10 处

5. **枚举必须前后端重复定义且被多处引用**
   - FoodCategory 和 FreshnessStatus 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 FoodItem 新增一个 'dried' 食品类别"，必须触达：GORM AutoMigrate、FoodItem 模型、前后端 constants、FoodService、FoodCard、FreshnessBadge、formatters、日志模板、错误码、看板筛选器、统计分组、智能推荐逻辑等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=cyfreshfood`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: cyfreshfood`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-cyfreshfood}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- Redis 服务随后端一起启动（`depends_on` 或后端启动前等待）
- 后端服务也配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `18628:80`，后端 `${BACKEND_PORT:-19628}:8080`，数据库 `5432:5432`，Redis `6379:6379`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=cyfreshfood`
- 数据库连接相关：`DB_NAME=cyfreshfood_db`、`DB_USER=cyfreshfood_user`、`DB_PASSWORD=cyfreshfood_pwd`
- Redis：`REDIS_HOST=redis`、`REDIS_PORT=6379`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=18628`、`BACKEND_PORT=19628`、`DB_PORT=5432`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
frontend/coverage/
uploads/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 FoodCategory 和 FreshnessStatus 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:18628` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<FreshnessBadge>` 或 `<FoodCard>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
